import React from 'react';
import { ProfileContainer, Title } from './style';

const ProfileScreen = () => {
  return (
    <ProfileContainer>
      <Title>Profile Page!</Title>
    </ProfileContainer>
  );
};

export default ProfileScreen;
